/// Shared HTTP client configuration.
/// Current implementations still use Dio / http package directly
/// inside scrapers and extractors (original behavior preserved).
class AppHttpClient {
  AppHttpClient._();
}
