class UserSettings {
  final String themeMode;
  final bool showDownloadsTab;
  final String preferredLanguage;
  const UserSettings({this.themeMode = 'dark', this.showDownloadsTab = true, this.preferredLanguage = 'es'});
}
