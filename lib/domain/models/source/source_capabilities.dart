class SourceCapabilities {
  final bool hasMovies;
  final bool hasSeries;
  final bool hasSearch;
  const SourceCapabilities({this.hasMovies = true, this.hasSeries = true, this.hasSearch = true});
}
