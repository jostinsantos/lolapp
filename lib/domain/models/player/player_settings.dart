class PlayerSettings {
  final bool autoPlayNext;
  final double playbackSpeed;
  final String? preferredQuality;
  const PlayerSettings({this.autoPlayNext = true, this.playbackSpeed = 1.0, this.preferredQuality});
}
