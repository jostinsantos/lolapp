import 'package:flutter/material.dart';

/// Visual constants – values match the original app (do not change).
class AppConstants {
  static const Color accentColor = Color(0xFFE50914);
  static const Color sideAccent = Color(0xFF7B5CFF);
  static const Color scaffoldBackground = Colors.black;
}
