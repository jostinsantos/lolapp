import 'package:flutter/material.dart';
import '../main.dart' show MyApp;
/// Entry point widget for the application.
class App extends StatelessWidget {
  const App({super.key});

  @override
  Widget build(BuildContext context) => const MyApp();
}
